const loadBtn = document.getElementById('btnLoad');
const creaeBtn = document.getElementById('btnCreate');
const tbody = document.getElementById('phonebook');
const personInput = document.getElementById('person');
const phoneInput = document.getElementById('phone');

export {
    loadBtn,
    creaeBtn,
    tbody,
    personInput,
    phoneInput
}